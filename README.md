# UpgradeLab Online V15 Fixed

Исправлено:
- выбор одного или нескольких скинов из инвентаря в ставку;
- конфликт `id` каталога и уникального `uid` инвентаря;
- кнопка «Применить» теперь находится снизу и показывает число выбранных предметов;
- можно выбрать карточку кликом по всей карточке;
- окно пополнения виртуального баланса больше не перекрывается кнопками и текстом;
- выравнивание и адаптация модальных окон.

Замените `app.js`, `styles.css` и их копии в `public/`. После деплоя обновите страницу через Ctrl+F5.
